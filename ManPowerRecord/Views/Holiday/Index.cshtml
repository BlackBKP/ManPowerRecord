@{ 
    ViewData["Title"] = "Holidays";
}
<div class="row p-3 d-flex align-items-center">
    <div class="col-xl-2 pb-3">
        <button id="btn_add" type="button" class="btn btn-primary form-control">
            <i class="fas fa-plus"></i> Add Holiday
        </button>
    </div>
    <div class="col-xl-1 pb-3">
        <button id="btn_import" type="button" class="btn btn-primary form-control">
            <i class="fas fa-upload"></i> Import
        </button>
    </div>
    <div class="col-xl-1 pb-3">
        <button id="btn_export" type="button" class="btn btn-primary form-control">
            <i class="fas fa-download"></i> Export
        </button>
    </div>
    <div class="col-xl-12">
        <div class="card">
            <div class="card-header">
                <span class="card-title">Holidays</span>
            </div>
            <div class="card-body">
                <div class="form-group">
                    <table id="table_holiday" class="table table-sm table-striped table-hover w-100">
                        <thead>
                            <tr>
                                <th>Date</th>
                                <th>Name</th>
                            </tr>
                        </thead>
                        <tfoot>
                            <tr>
                                <th>Date</th>
                                <th>Name</th>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="modal_holiday" tabindex="-1" role="dialog" aria-hidden="true" data-backdrop="static">
    <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Add Holiday</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form>
                    <div class="form-group">
                        <label for="date">Date</label>
                        <input id="date" type="date" class="form-control" />
                    </div>
                    <div class="form-group">
                        <label for="holiday_name">Holiday Name</label>
                        <input id="holiday_name" type="text" class="form-control" />
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                <button id="btn_save" type="button" class="btn btn-primary">Save changes</button>
            </div>
        </div>
    </div>
</div>

@section Scripts
{ 
    <script type="text/javascript">
        var table;

        $(document).ready(function () {
            GetHolidays();
        });

        function GetHolidays() {
            $.ajax({
                type: "GET",
                url: '@Url.Action("GetHolidays", "Holiday")',
	            contentType: 'application/x-www-form-urlencoded',
                data: {
                },
                success: function (response) {
                    GenerateTable(response);
                }
            });
        }

        function GenerateTable(holidays) {
            var datas = [];

            for (var i = 0; i < holidays.length; i++) {
                datas.push([
                    holidays[i].date.split("T")[0],
                    holidays[i].name
                ]);
            }

            table = $('#table_holiday').DataTable({
                iDisplayLength: -1,
                info: false,
                paging: false,
                ordering: false,
                searching: false,
                data:datas,
            });
        }


        $('#btn_add').on('click', function () {
            $('#modal_holiday').modal();
        });
    </script>
}